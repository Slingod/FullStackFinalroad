<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Tous les Événements</title>
    <meta name="description" content="Découvrez tous les événements sur Nouvelle Ère Lissoise. Consultez les détails de chaque événement, y compris le titre, l'auteur, le lieu, la date, la durée et le prix.">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <div class="events-container">
    <h1>Tous les Événements</h1>

    <% @events.each do |event| %>
      <div class="event-card">
        <h2><%= event.title %></h2>
        <p><strong>Auteur:</strong> <%= event.author %></p>
        <p><strong>Emplacement:</strong> <%= event.location %></p>
        <p><strong>Date:</strong> <%= event.date.strftime("%d %B %Y") %></p>
        <p><strong>Durée:</strong> <%= event.duration %> Heures</p>
        <p><strong>Prix:</strong> <%= event.price %> €</p>
        <%= link_to 'Voir l\'événement', event_path(event), class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</body>
</html>