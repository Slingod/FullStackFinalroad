<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Accueil - Nouvelle Ère Lissoise</title>
    <meta name="description" content="Rejoignez-nous et partagez nos valeurs à travers nos événements. Découvrez l’événement en cours ou le prochain à venir !">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <main class="auth-container">
    <h1>Bienvenue sur Nouvelle Ère Lissoise</h1>
    <hr>
    <p>
      <strong>Chers utilisateurs,</strong><br>
      Nous vous invitons à nous rejoindre et à partager nos valeurs au sein de nos événements.
      Vous retrouverez ci-dessous l'événement en cours ou le prochain à venir. Nous vous attendons, petits et grands !
    </p>

    <% if @next_event %>
      <section class="featured-event">
        <h2>Prochain Événement</h2>
        <article class="event-card">
          <div class="card-body">
            <strong class="card-title"><%= @next_event.author %></strong>
            <p class="card-text"><strong>Lieu :</strong> <%= @next_event.location %></p>
            <!-- Manually mapping the month name to French -->
            <% month_names = {
              "January" => "janvier", "February" => "février", "March" => "mars", "April" => "avril",
              "May" => "mai", "June" => "juin", "July" => "juillet", "August" => "août",
              "September" => "septembre", "October" => "octobre", "November" => "novembre", "December" => "décembre"
            } %>
            <p class="card-text"><strong>Date :</strong> <%= @next_event.date.strftime("%d") %> <%= month_names[@next_event.date.strftime("%B")] %> <%= @next_event.date.strftime("%Y") %></p>
            <p class="card-text"><strong>Durée :</strong> <%= @next_event.duration %> minutes</p>
            <p class="card-text"><strong>Prix :</strong> <%= @next_event.price %> €</p>
            <div class="event-actions">
              <a href="/events/<%= @next_event.id %>" class="btn btn-primary">Voir l'Événement</a>
            </div>
          </div>
        </article>
      </section>
    <% else %>
      <p>Aucun événement prévu pour le moment.</p>
    <% end %>

    <!-- Bouton pour voir plus d'événements -->
    <div class="event-actions">
      <a href="<%= events_path %>" class="btn btn-success">
        👉 Pour plus d'événements, cliquez ici !
      </a>
    </div>
  </main>
</body>
</html>