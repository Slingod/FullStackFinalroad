<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Contactez-Nous</title>
    <meta name="description" content="Contactez Nouvelle Ère Lissoise via ce formulaire. Remplissez votre nom, email et message pour nous envoyer vos questions ou commentaires.">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <section class="contact-container">
    <h1 class="contact-title">Contactez-Nous</h1>

    <%= form_with model: @contact, url: contacts_path, local: true do |form| %>
      <div class="contact-field">
        <%= form.label :name, "Nom" %>
        <%= form.text_field :name, class: "contact-input" %>
      </div>
      <div class="contact-field">
        <%= form.label :email, "Email" %>
        <%= form.email_field :email, class: "contact-input" %>
      </div>
      <div class="contact-field">
        <%= form.label :message, "Message" %>
        <%= form.text_area :message, class: "contact-textarea" %>
      </div>
      <div class="contact-actions">
        <%= form.submit "Envoyer", class: "btn btn-primary" %>
      </div>
    <% end %>
  </section>
</body>
</html>