<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Contactez-Nous</title>
    <meta name="description" content="Contactez Nouvelle Ère Lissoise via ce formulaire. Remplissez votre nom, email et message pour nous envoyer vos questions ou commentaires.">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags %> <!-- Sécurité CSRF -->
</head>
<body>
  <section class="contact-container">
    <h1 class="contact-title">Contactez-Nous</h1>

    <%= form_with model: @contact, url: contacts_path, method: :post do |f| %>
      <div class="contact-field">
        <%= f.label :name, "Nom" %>
        <%= f.text_field :name, class: "contact-input" %>
      </div>
      <div class="contact-field">
        <%= f.label :email, "Email" %>
        <%= f.email_field :email, class: "contact-input" %>
      </div>
      <div class="contact-field">
        <%= f.label :message, "Message" %>
        <%= f.text_area :message, class: "contact-textarea" %>
      </div>
      <div class="contact-actions">
        <%= f.submit "Envoyer", class: "btn btn-primary" %>
      </div>
    <% end %>
  </section>
</body>
</html>