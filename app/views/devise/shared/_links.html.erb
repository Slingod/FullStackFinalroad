<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Navigation - Nouvelle Ère Lissoise</title>
    <meta name="description" content="Accédez rapidement aux options de connexion, d'inscription et de récupération de compte sur Nouvelle Ère Lissoise.">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <nav class="auth-links">
    <%- if controller_name != 'sessions' %>
      <%= link_to "Se Connecter", new_session_path(resource_name), class: "auth-link" %><br />
    <% end %>

    <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
      <%= link_to "S'Inscrire", new_registration_path(resource_name), class: "auth-link" %><br />
    <% end %>

    <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
      <%= link_to "Mot de Passe Oublié ?", new_password_path(resource_name), class: "auth-link" %><br />
    <% end %>

    <%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
      <%= link_to "Instructions de Confirmation Non Reçues ?", new_confirmation_path(resource_name), class: "auth-link" %><br />
    <% end %>

    <%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
      <%= link_to "Instructions de Déverrouillage Non Reçues ?", new_unlock_path(resource_name), class: "auth-link" %><br />
    <% end %>

    <%- if devise_mapping.omniauthable? %>
      <%- resource_class.omniauth_providers.each do |provider| %>
        <%= button_to "Se Connecter avec #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), data: { turbo: false }, class: "auth-btn" %><br />
      <% end %>
    <% end %>
  </nav>
</body>
</html>