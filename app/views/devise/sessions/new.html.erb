<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Connexion - Nouvelle Ère Lissoise</title>
    <meta name="description" content="Connectez-vous à votre compte Nouvelle Ère Lissoise pour accéder à nos services et interagir avec la communauté.">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <section class="auth-container">
    <h2 class="auth-title">Connexion</h2>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "auth-form" }) do |f| %>
      <div class="auth-field">
        <%= f.label :email, "Email" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "auth-input", aria_label: "Adresse email" %>
      </div>

      <div class="auth-field">
        <%= f.label :password, "Mot de passe" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "auth-input", aria_label: "Mot de passe" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="auth-field remember-me">
          <%= f.check_box :remember_me, class: "auth-checkbox", id: "remember_me" %>
          <%= f.label :remember_me, "Se souvenir de moi", for: "remember_me" %>
        </div>
      <% end %>

      <div class="auth-actions">
        <%= f.submit "Se Connecter", class: "btn btn-primary" %>
        <%= link_to "Mot de passe oublié ?", new_password_path(resource_name), class: "btn btn-success" %>
        <%= link_to "S'inscrire", new_registration_path(resource_name), class: "btn btn-danger" %>
      </div>
    <% end %>
  </section>
</body>
</html>